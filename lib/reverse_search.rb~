module Reverse
	class Search
		def self.find(regxp) 
			file = File.open("/home/"+ENV['USER']+"/.bash_history", "r")
			all_lines = file.readlines
			puts all_lines.select {| line | line.include?(regexp) }
		end
	end
end
